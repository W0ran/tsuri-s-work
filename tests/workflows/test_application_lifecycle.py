import pytest


@pytest.mark.workflows
@pytest.mark.skip(
    reason=(
        "Сквозной сценарий (заявитель создаёт заявку -> эксперт проверяет -> "
        "председатель/совет утверждает) ещё не спроектирован — требует UI "
        "экспертa и совета (сейчас не изучен) и согласования, в каком "
        "статусе заявка должна появляться у каждой следующей роли."
    )
)
def test_application_lifecycle_placeholder():
    pass
